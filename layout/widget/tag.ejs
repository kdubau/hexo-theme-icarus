<% if (site.tags.length) { %>
    <div class="widget-wrap widget-card">
        <h3 class="widget-title"><%= __('widget.tags') %></h3>
        <div class="widget">
            <%- list_tags() %>
        </div>
    </div>
<% } %>